# Vercel Deploy Gate Docs-Only Proof

This file exists only to prove that documentation-only pull requests are
classified as non-frontend changes by the Vercel deploy gate.

Expected gate decision:

- `deploy=false`

This proof should not require the gated Vercel CLI deployment job.
