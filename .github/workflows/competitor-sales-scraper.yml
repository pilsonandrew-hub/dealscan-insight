name: Competitor Sales Scraper

# Daily scrape of completed/sold auction listings (GovDeals, PublicSurplus,
# GovPlanet) into the competitor_sales table that feeds DOS ceiling pricing.

on:
  schedule:
    - cron: '0 9 * * *'   # 09:00 UTC daily (~1-2am PT)
  workflow_dispatch:
    inputs:
      sources:
        description: "Comma list of sources (govdeals,publicsurplus,govplanet). Blank = all."
        required: false
        default: ""
      max_pages:
        description: "Max result pages per source"
        required: false
        default: "10"
      dry_run:
        description: "Scrape & report without writing to Supabase"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      govdeals_seo_asset_urls:
        description: "Optional newline/comma-separated GovDeals SEO asset URLs for sold-page fallback proof"
        required: false
        default: ""

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  SCHEDULED_COMPETITOR_SCRAPER_SOURCES: govdeals
  SCHEDULED_COMPETITOR_SCRAPER_MAX_PAGES: "1"
  SCHEDULED_COMPETITOR_GOVDEALS_SEO_ASSET_URLS: https://prod-seo.govdeals.com/en/asset/17167/7167

jobs:
  competitor-sales-scraper:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --disable-pip-version-check --no-cache-dir \
            supabase==2.30.0 httpx==0.27.2 beautifulsoup4==4.12.2 lxml==4.9.3 \
            "playwright>=1.40.0,<2.0.0"
          python -m playwright install --with-deps chromium

      - name: Run competitor sales scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          COMPETITOR_SCRAPER_SOURCES: ${{ inputs.sources }}
          COMPETITOR_SCRAPER_MAX_PAGES: ${{ inputs.max_pages || '10' }}
          COMPETITOR_SCRAPER_DRY_RUN: ${{ inputs.dry_run || 'false' }}
          COMPETITOR_GOVDEALS_SEO_ASSET_URLS: ${{ inputs.govdeals_seo_asset_urls || '' }}
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "schedule" ]; then
            COMPETITOR_SCRAPER_SOURCES="${SCHEDULED_COMPETITOR_SCRAPER_SOURCES}"
            COMPETITOR_SCRAPER_MAX_PAGES="${SCHEDULED_COMPETITOR_SCRAPER_MAX_PAGES}"
            COMPETITOR_GOVDEALS_SEO_ASSET_URLS="${SCHEDULED_COMPETITOR_GOVDEALS_SEO_ASSET_URLS}"
            export COMPETITOR_SCRAPER_SOURCES COMPETITOR_SCRAPER_MAX_PAGES COMPETITOR_GOVDEALS_SEO_ASSET_URLS
          fi
          python scripts/run_competitor_sales_scraper.py
