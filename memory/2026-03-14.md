# Memory — 2026-03-14

## Morning Session — ~6:45–8:59 AM PDT

### DealerScope 12-fix verification and follow-up
- Andrew asked to address all issues found jointly by me and Codex, then verify they were truly fixed.
- Three parallel Codex sessions were launched to fix the initial 12 issues.
- Initial 12-fix commit landed as `9236ab3`.
- Verification found 3 remaining defects in that batch:
  1. Crosshair dedup filter needed tightening
  2. Rover Redis affinity dimensions needed full payload
  3. `auction_fees` was still double-counting premium
- Follow-up fix commit landed as `dc4a054`.

### Codex review of the 12 fixes
- Andrew asked for a hostile Codex review: not just whether fixes worked, but whether they were optimal.
- Codex rated fixes 3, 4, 8, 9, and 10 as needing improvement; 10 was flagged as a hard security failure.
- Main findings:
  - `scripts/deal-alerts.sh` had committed Supabase service-role JWT + Telegram bot token and disabled TLS verification.
  - `/api/pipeline/run` returned HTTP 200 "disabled" and no longer required auth; old clients could mistake it for success.
  - `SECRET_KEY` fallback still allowed a known dev key in production.
  - Dashboard still had at least one component rendering avg margin as a percent.
  - `recon_reserve` and canonical `total_cost` were not persisted for UI-side all-in economics.

### Follow-up security + correctness fixes
- Andrew approved letting Codex drive the fixes while I reviewed.
- Codex removed secrets from `deal-alerts.sh`, switched it to env vars only, restored verified TLS, added required env validation, and stopped embedding tokens.
- Ingest now fails closed in production when `SUPABASE_SERVICE_ROLE_KEY` is missing; anon fallback allowed only in development.
- `/api/pipeline/run` now returns `410 Gone` with a clear retirement message instead of HTTP 200.
- `avg_margin` in `DashboardMetrics.tsx` now renders as dollars, not percent.
- `recon_reserve` and `total_cost` are now persisted to the opportunities row; migration created at `supabase/migrations/20260314_total_cost.sql`.
- Follow-up commit landed as `7b69564`.

### Important operator action still needed
- I warned Andrew that two secrets exposed in git history still need rotation even though they were removed from the current file:
  - Telegram bot token
  - Supabase service role key

### Scraper alert false positive
- Andrew received a "DealerScope Scraper Stalled" alert for Apify parseforge task `w0HvYYMmtgeKIGYe6`.
- Verified this is a dead/legacy Parseforge monitor, not the live DealerScope actors.
- Confirmed real live actors were healthy:
  - GovDeals actor `CuKaIAcWyFS0EPrAz` succeeded at 3:00 AM PT
  - PublicSurplus actor `9xxQLlRsROnSgA42i` succeeded at 3:00 AM PT
- Decision: treat the parseforge stall alert as noise and later remove/update that monitoring rule.

### Variable-management manifesto strategy
- Andrew sent the DealerScope variable-management manifesto again and asked me + Codex to design the implementation strategy.
- Codex analyzed manifesto vs current code and we aligned on a 4-sprint roadmap.
- Key Sprint 1 decisions (approved by Andrew):
  - `retail_asking_price_estimate = mmr_estimated * 1.35`
  - Keep wholesale CtM and add retail CtM proxy
  - MDS proxy buckets: Tier1=25d, Tier2=35d, Tier3=50d, Tier4=70d
  - `roi_per_day = gross_margin / estimated_days_to_sale`
  - Replace DOS-first ranking with institutional proxy score
  - Dynamic all-in bid ceilings:
    - Platinum Tier1 => 88%
    - Gold Tier1/2 => 85%
    - Silver => 82%
    - Bronze => reject
  - Dashboard should become grade-first and surface grade/CtM/ROI-day/headroom
  - Add Platinum alerts and suppress generic DOS alerts when Platinum qualifies
- I explicitly recommended not waiting for Manheim API to start Sprint 1.

### Sprint 1 implementation started
- Andrew said: "Proceed with implementing."
- I launched Codex session `mild-cloud` in `/Users/andrewpilson/.openclaw/workspace/projects/dealerscope/` to implement Sprint 1.
- Scope given to Codex:
  - backend scoring proxy upgrade
  - post-score all-in ceiling logic
  - schema + migration
  - API + type plumbing
  - pragmatic dashboard upgrade
  - Platinum-aware alerts
- Before compaction, Codex had completed code-path review and begun edits. Confirmed findings from the run:
  - current 88% ceiling still happens too early in ingest flow
  - frontend `total_cost` mapping still needs correction
  - persistence and alert paths are identified cleanly
- Session still in progress at flush time: `mild-cloud` / Codex session id `019ced04-bb58-7192-9e71-ccf3840695e8`.
