{
  "title": "Proxibid Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "maxItems": {
      "title": "Max Items",
      "type": "integer",
      "description": "Maximum number of items to collect",
      "default": 200,
      "minimum": 1,
      "editor": "number"
    },
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "Vehicle search keyword (e.g. Tesla Model 3)",
      "editor": "textfield",
      "default": ""
    },
    "maxDetailPages": {
      "title": "Max Detail Pages",
      "type": "integer",
      "description": "Maximum Proxibid detail pages to fetch per run for VIN/mileage enrichment before pushing rows",
      "default": 200,
      "minimum": 0,
      "maximum": 250,
      "editor": "number"
    }
  }
}
