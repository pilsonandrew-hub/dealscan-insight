name: Configure JJKane Apify Env

on:
  workflow_dispatch:
    inputs:
      schedule_cron:
        description: "Apify UTC cron for JJKane cadence"
        required: false
        default: "0 */12 * * *"

jobs:
  configure-jjkane-apify-env:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
      APIFY_WEBHOOK_SECRET: ${{ secrets.APIFY_WEBHOOK_SECRET }}
      MARKETCHECK_KEY_1: ${{ secrets.MARKETCHECK_KEY_1 }}
      MARKETCHECK_KEY_2: ${{ secrets.MARKETCHECK_KEY_2 }}
      MARKETCHECK_KEY_3: ${{ secrets.MARKETCHECK_KEY_3 }}
      ACTOR_ID: "lvb7T6VMFfNUQpqlq"
      SCHEDULE_NAME: "ds-jjkane-12hr"
      DEFAULT_SCHEDULE_CRON: "0 */12 * * *"
      SCHEDULE_CRON: ${{ inputs.schedule_cron || '0 */12 * * *' }}
    steps:
      - name: Configure actor secret env and Apify schedule
        run: |
          python3 - <<'PY'
          import json
          import os
          import sys
          import time
          import urllib.error
          import urllib.parse
          import urllib.request

          APIFY_TOKEN = os.environ.get("APIFY_TOKEN", "").strip()
          APIFY_WEBHOOK_SECRET = os.environ.get("APIFY_WEBHOOK_SECRET", "").strip()
          ACTOR_ID = os.environ["ACTOR_ID"]
          SCHEDULE_NAME = os.environ["SCHEDULE_NAME"]
          DEFAULT_SCHEDULE_CRON = os.environ["DEFAULT_SCHEDULE_CRON"]
          SCHEDULE_CRON = (os.environ.get("SCHEDULE_CRON") or DEFAULT_SCHEDULE_CRON).strip()
          MARKETCHECK_KEYS = [
              ("MARKETCHECK_KEY_1", os.environ.get("MARKETCHECK_KEY_1", "").strip()),
              ("MARKETCHECK_KEY_2", os.environ.get("MARKETCHECK_KEY_2", "").strip()),
              ("MARKETCHECK_KEY_3", os.environ.get("MARKETCHECK_KEY_3", "").strip()),
          ]

          if not APIFY_TOKEN:
              raise SystemExit("APIFY_TOKEN GitHub secret is required")
          if not APIFY_WEBHOOK_SECRET:
              raise SystemExit("APIFY_WEBHOOK_SECRET GitHub secret is required")
          if not any(value for _, value in MARKETCHECK_KEYS):
              raise SystemExit("At least one MARKETCHECK_KEY_N GitHub secret is required")

          def request(method, path, body=None):
              url = f"https://api.apify.com/v2{path}"
              data = None if body is None else json.dumps(body).encode("utf-8")
              req = urllib.request.Request(
                  url,
                  data=data,
                  method=method,
                  headers={
                      "Authorization": f"Bearer {APIFY_TOKEN}",
                      "Content-Type": "application/json",
                  },
              )
              try:
                  with urllib.request.urlopen(req, timeout=30) as response:
                      return json.load(response).get("data")
              except urllib.error.HTTPError as exc:
                  detail = exc.read().decode("utf-8", errors="replace")
                  raise RuntimeError(f"{method} {path} failed: HTTP {exc.code}: {detail}") from exc

          def paged_items(path):
              data = request("GET", path)
              if isinstance(data, dict) and "items" in data:
                  return data["items"]
              return []

          def wait_for_terminal_build(build_id):
              terminal = {"SUCCEEDED", "FAILED", "ABORTED", "TIMED-OUT"}
              last = {}
              for _ in range(90):
                  last = request("GET", f"/actor-builds/{build_id}")
                  status = last.get("status")
                  print(json.dumps({
                      "configured_build_id": build_id,
                      "configured_build_status": status,
                      "configured_build_number": last.get("buildNumber"),
                  }, sort_keys=True))
                  if status in terminal:
                      if status != "SUCCEEDED":
                          raise SystemExit(f"JJKane build {build_id} ended with {status}")
                      return last
                  time.sleep(10)
              raise SystemExit(f"Timed out waiting for JJKane build {build_id}")

          versions = paged_items(f"/actors/{ACTOR_ID}/versions?limit=100")
          if not versions:
              raise SystemExit(f"No versions found for actor {ACTOR_ID}")

          def version_sort_key(version_number):
              parts = []
              for part in str(version_number).split("."):
                  try:
                      parts.append(int(part))
                  except ValueError:
                      parts.append(-1)
              return tuple(parts)

          active_version = max(versions, key=lambda version: version_sort_key(version["versionNumber"]))
          active_version_number = active_version["versionNumber"]

          configured_versions = []
          for version in versions:
              version_number = version["versionNumber"]
              env_path = (
                  f"/actors/{ACTOR_ID}/versions/"
                  f"{urllib.parse.quote(version_number, safe='')}/env-vars"
              )
              env_vars = paged_items(env_path)
              existing_names = {item.get("name") for item in env_vars}
              desired_env = [
                  ("WEBHOOK_SECRET", APIFY_WEBHOOK_SECRET),
                  *[(name, value) for name, value in MARKETCHECK_KEYS if value],
              ]
              for name, value in desired_env:
                  payload = {"name": name, "value": value, "isSecret": True}
                  if name in existing_names:
                      request("PUT", f"{env_path}/{urllib.parse.quote(name, safe='')}", payload)
                  else:
                      request("POST", env_path, payload)
              configured_versions.append(version_number)

          build = request(
              "POST",
              f"/actors/{ACTOR_ID}/builds?version={active_version_number}"
              "&tag=latest&useCache=false&betaPackages=false",
          )
          build = wait_for_terminal_build(build["id"])

          schedule_body = {
              "name": SCHEDULE_NAME,
              "title": "JJKane 12h source-quality run",
              "description": (
                  "DealerScope JJKane cadence. Marketcheck and webhook secrets are "
                  "provided by actor secret environment variables, not schedule input."
              ),
              "isEnabled": True,
              "isExclusive": True,
              "cronExpression": SCHEDULE_CRON,
              "timezone": "UTC",
              "actions": [
                  {
                      "type": "RUN_ACTOR",
                      "actorId": ACTOR_ID,
                      "runInput": {
                          "body": json.dumps({}, sort_keys=True),
                          "contentType": "application/json; charset=utf-8",
                      },
                      "runOptions": {
                          "build": "latest",
                          "timeoutSecs": 300,
                          "memoryMbytes": 1024,
                      },
                  }
              ],
          }

          schedules = paged_items("/schedules?limit=1000")
          existing = next((item for item in schedules if item.get("name") == SCHEDULE_NAME), None)
          if existing:
              schedule = request("PUT", f"/schedules/{existing['id']}", schedule_body)
          else:
              schedule = request("POST", "/schedules", schedule_body)

          print(json.dumps({
              "actor_id": ACTOR_ID,
              "configured_versions": configured_versions,
              "active_version": active_version_number,
              "build_id": build.get("id"),
              "build_number": build.get("buildNumber"),
              "schedule_id": schedule.get("id"),
              "schedule_name": schedule.get("name"),
              "schedule_enabled": schedule.get("isEnabled"),
              "schedule_cron": schedule.get("cronExpression"),
          }, sort_keys=True))
          PY
