# Backend development and test dependencies
# Keep this intentionally narrow so local analytics/backend tests do not pull
# the full production stack (ML, browser automation, etc.).

fastapi==0.115.0
pydantic==2.8.0
pydantic-settings==2.6.0
PyJWT>=2.8.0
sqlalchemy==2.0.23
supabase==2.10.0
python-dotenv==1.0.0
PyYAML==6.0.1
pytest==8.3.5
