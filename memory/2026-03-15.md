## DealerScope live ingest debugging
- Fixed production DB access by using direct Supabase DB host `db.lbnxzvqppccajllsqaaw.supabase.co:5432` with user `postgres`; Supabase pooler path was failing with `Tenant or user not found`.
- Applied missing March 2026 `supabase/migrations/*` files to production; live `public.opportunities` schema verification then passed.
- Identified and fixed production ingest contract mismatch: Apify webhooks could omit `resource.defaultDatasetId`, causing Railway ingest to return `{"status":"ok","message":"No dataset to process"}`. Patched `webapp/routers/ingest.py` to resolve dataset id from actor run when missing, committed and pushed on `main` as `3c2ac1a` (`fix: resolve missing Apify dataset ids from actor runs`), and confirmed Railway deployed it successfully.
- After deploy, live Railway ingest began processing replayed runs instead of returning `No dataset to process`, proving the webhook dataset-id fix is live.
- Current live blocker as of 2026-03-15: processed items still do not persist to `public.opportunities` because Supabase REST/PostgREST insert path is out of sync with schema cache. Railway logs show `PGRST204` errors like missing `city`, `buyer_premium`, `bid_ceiling_pct`, and `acquisition_basis_source` in schema cache, even though those columns exist in Postgres and `NOTIFY pgrst, 'reload schema'` was already attempted.
- Best next unblock path identified: either bypass Supabase REST for ingest writes and write directly to Postgres, or force/reset Supabase API schema cache from the Supabase side.
- Implemented direct Postgres fallback for ingest saves and normalized PublicSurplus relative auction-end strings into anchored timestamps; these changes were committed to `main` as `3f0de77` (`fix: add direct db fallback for ingest saves`) and `292174f` (`fix: normalize relative auction end times`).
- Hardened ingest observability and replay semantics across several commits: `d4b7ee3` (better structured ingest reporting), `d2b3e72` (replay/save accounting hardening), `674f597` (stable hashed `listing_id`, replay/timestamp hardening, Notion replay safety), `73eb3c2` (timezone import for timestamp normalization), `84316ad` (new `ingest_delivery_log` ledger), and `6926f5b` (defer canonical duplicate updates until save succeeds).
- Live validation on 2026-03-15 confirmed commit `6926f5b` deployed successfully to Railway and did not regress ingestion; GovDeals replay `RTOldLh9p2hsBbf4q` returned `processed=1 existing=1 inserted=0`, and PublicSurplus replay `bv8CAuvsGXdaM4hJ6` returned `processed=4 existing=4 inserted=0`, with ledger rows showing `db_save=duplicate_existing` and `notion_sync=sent` as expected.
- Verified production delivery ledger path is now real: `ingest_delivery_log` records per-channel completion state for `db_save`, `notion_sync`, and `telegram_alert`. For opportunity `2fb9e5b4-da89-4790-9cbb-180e4901a3bb` in run `bv8CAuvsGXdaM4hJ6`, confirmed a `telegram_alert` ledger row with listing id `c570e18b67c019dea75046f092d6c7fb71705cb2`, status `sent`, external id `987654321`, plus matching `alert_log` receipt.
- Conclusion from the March 15 recovery work: DealerScope ingest is now "operationally fixed and materially hardened"—live pipeline restored, automatic PublicSurplus landings confirmed, GovDeals pipeline healthy, reporting more truthful, and side effects replay-safe via explicit ledgering.
- Andrew pushed back on a passive "Monday-style" follow-up plan and prefers implementing the remaining hardening immediately rather than waiting 24 hours; recommended next active work is to do tests/docs/ops improvements today while scheduled runs provide passive live verification in parallel.
